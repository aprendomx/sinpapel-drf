from django.apps import AppConfig


class SinpapelDrfConfig(AppConfig):
    name = "sinpapel_drf"
    verbose_name = "Sinpapel DRF (HTTP layer)"
    default_auto_field = "django.db.models.BigAutoField"
